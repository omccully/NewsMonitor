<local:SettingsPage x:Class="NewsMonitor.WPF.Views.GeneralSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NewsMonitor.WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GeneralSettingsPage">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto"></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"></RowDefinition>
      <RowDefinition Height="auto"></RowDefinition>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <Label HorizontalAlignment="Right">Search Terms (separated by commas):</Label>
    <TextBox x:Name="SearchTermsTextBox" Grid.Column="1"></TextBox>
    <Label HorizontalAlignment="Right" 
           Grid.Row="1" Grid.Column="0">Max Article Age (days):</Label>
    <TextBox x:Name="MaxArticleAgeDaysTextBox" 
             HorizontalAlignment="Left" Width="100" Grid.Row="1" Grid.Column="1"></TextBox>
  </Grid>
</local:SettingsPage>
